@model IEnumerable<UNO.AppModel.EmployeeTimeAttendanceCreateModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section scripts
{
    <script type="text/javascript">
    var createNewPage = '@Url.Action("Create", "EmployeeTimeAttendance")';
    $(document).ready(function () {

        // $('#data-table').DataTable();

        if ('@ViewBag.CreateAccess' == "False") {
            $(".btnCreateNew").hide();
        }
        if ('@ViewBag.ViewAccess' == "False") {
            $(".btn-success").hide();
        }
        if ('@ViewBag.EditAccess' == "False") {
            $(".btn-info").hide();
        }
        if ('@ViewBag.DeleteAccess' == "False") {
            $(".btn-danger").hide();
        }

        $('#lsttable').dataTable({
            "sPaginationType": "full_numbers",
            dom: 'lBfrtip',
            "bInfo": true,
            oLanguage: {
                sLengthMenu: "Show: _MENU_",
            },
            columns: [
                null,
                null,
                null,
                null,
                null,
                null,
                {
                    "searchable": false,
                    "orderable": false
                }
            ],
            buttons: [
                {
                    text: '<i class="fa fa-plus-circle"></i> Add New',
                    action: function myFunction() {
                        location.href = createNewPage;
                    },
                    attr: {
                        id: 'btnCreateNew'
                    }
                }
        ]

    });

    });

</script>
}










<!-- Container fluid Starts -->
<div class="container-fluid">
    <!-- Spacer Starts -->
    <div class="spacer clearfix">
        <div class="row">
            <div class="col-md-12">
                <div class="blog blog-default ">
                    @*<div class="blog-header ">
                                                        <h4 class="blog-title pull-left mt5">Listing</h4><div class="dt-buttons pull-right">
                            @Html.ActionLink(" Create New", "Create", "Menu", null, new { @id = "btnCreate", @class = "fa fa-plus-circle text-white btn btn-warning pull-right" })<a class="btn btn-default" href="#"><span><i class="fa fa-file-excel-o"></i> Excel</span></a></div>
                        </div>*@

                    <div class="blog-header ">
                        <h4 class="blog-title pull-left mt5">Listing</h4>
                        <div class="dt-buttons pull-right">
                            <a class="btn btn-default" href="#"><span><i class="fa fa-print"></i> Print</span></a> <a class="btn btn-default" href="#"><span><i class="fa fa-file-excel-o"></i> Excel</span></a>
                        </div>
                        <span class="clearfix"></span>
                    </div>
                    <div class="blog-body">
                        <div class="table-responsive">
                            <div id="dt_example" class="table-responsive example_alt_pagination clearfix">
                                <table id="lsttable" class="table table-condensed table-striped table-hover table-bordered pull-left">
                                    <thead>
                                        <tr>
                                            <th class="text-center">Employee Code</th>
                                            <th class="text-center">Minimum Swipe</th>
                                            <th class="text-center">Shift Code</th>
                                            <th class="text-center">WEEKEND</th>
                                            <th class="text-center">WEEKOFF</th>
                                            <th class="text-center">SHIFT START DATE</th>
                                            
                                            <th class="text-center">ACTION</th>

                                        </tr>
                                    </thead>
                                    <tbody id="data">
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_EMP_ID)
                                                </td>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_MINIMUM_SWIPE)
                                                </td>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_SHIFTCODE)
                                                </td>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_WEEKEND)
                                                </td>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_WEEKOFF)
                                                </td>
                                                <td class="text-center">
                                                    @Html.DisplayFor(modelItem => item.ETC_SHIFT_START_DATE)
                                                </td>
                                                
                                                <td class="text-center" width="15%">
                                                    @Html.ActionLink(" ", "View", new { id = item.ETC_EMP_ID }, new { @class = "fa fa-eye btn btn-success btn-sm" })
                                                    @*@Html.ActionLink(" ", "DeleteView", new { id = item.MENU_ID }, new { @class = "fa fa-eye btn btn-success btn-sm" })&nbsp;*@
                                                    @Html.ActionLink(" ", "Edit", new { id = item.ETC_EMP_ID }, new { @class = "fa fa-edit btn btn-info" })&nbsp;
                                                    @Html.ActionLink(" ", "DeleteView", new { id = item.ETC_EMP_ID }, new { @class = "fa fa-trash-o btn btn-danger" })
                                                </td>
                                            </tr>
                                        }

                                    </tbody>

                                </table>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Spacer Ends -->
</div>

